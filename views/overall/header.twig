{% set links = [
    {title: 'головна' , href: site.url ~ '' },
    {title: 'проекти' , href: site.url ~ 'projects'},
    {title: 'графіка' , href: site.url ~ 'graphics'},
    {title: 'про мене' , href: site.url ~ 'about'},
] %}
<header>
    <div class="inner decorBorder leftToBottom">
        {% include 'overall/logo.twig' %}
        <nav>
            <ul class="desktopMenu desktop-menu">
            {% for link in links %}
                <li><a href="{{ link.href }}">{{ link.title }}</a></li>
            {% endfor %}
                <li class="navThumb nav-thumb" aria-hidden="true"></li>
            </ul>
            <ul class="mobileMenu mobile-menu decorBorder rightToTop">
            {% for link in links %}
                <li><a href="{{ link.href }}">{{ link.title }}</a></li>
            {% endfor %}
            </ul>
            <button class="burger burger-toggler"><span></span></button>
        </nav>
    </div>
</header>
