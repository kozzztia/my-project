{% extends "overall/block-base.twig" %}

{% block block %}

    {% if fields.text %}
    <article class="text typo {% if fields.class %} {{ fields.class }} {% endif %}" {% if fields.style %} style="{{ fields.style }}" {% endif %}>
    {{ fields.text }}
    </article>
    {% endif %}

{% endblock %}
