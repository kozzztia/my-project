{% extends "overall/block-base.twig" %}

{% block block %}

    {% if fields.text %}
    <div class="text-or-title typo {% if fields.class %}{{ fields.class }}{% endif %}" {% if fields.style %}style="{{ fields.style }}"{% endif %}>
        {{ fields.text }}
    </div>
    {% endif %}

{% endblock %}
